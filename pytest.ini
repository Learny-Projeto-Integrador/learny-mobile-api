[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -ra
    --strict-markers
    --tb=short
    --cov=api
    --cov-report=term-missing
    --cov-report=xml
filterwarnings =
    ignore::DeprecationWarning
